import { FC, ReactNode } from "react";
type Props = {
    source: string;
    ratio: number;
    className?: string;
    extraButtons?: ReactNode;
};
declare const SignVideo: FC<Props>;
export default SignVideo;
